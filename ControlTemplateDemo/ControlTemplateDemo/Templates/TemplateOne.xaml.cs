using System;
using System.Collections.Generic;
using Xamarin.Forms;

namespace ControlTemplateDemo.Templates
{
    public partial class TemplateOne : Frame
    {
        public TemplateOne()
        {
            InitializeComponent();

            //ShellContent shellContent = new ShellContent();
            //shellContent.ContentTemplate = new DataTemplate(typeof(MainPage));
        }
    }
}
